# DONKEY API URL (로컬에서 원격 API 쓸 때 또는 분리 배포 시)
# 비우면 개발 시 Vite 프록시(/api → localhost:8000), 프로덕션은 상대 경로
# 예: https://donkey.ai.kr
VITE_API_BASE=

# 개발 전용: UI만 볼 때 (로그인·API 없이). 프로덕션 빌드에서는 적용 안 됨
# VITE_DEV_PREVIEW=true
# VITE_DEV_PREVIEW_ROLE=admin
